import 'package:flutter/material.dart';

class AppConstants {
  static const String demoTeamId = 'demo-team';
  static const Duration silenceThreshold = Duration(minutes: 30);
  static const Duration ackDefault = Duration(minutes: 10);
  static const EdgeInsets pagePadding = EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 16,
  );
}
