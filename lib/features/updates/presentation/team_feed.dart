import 'package:flutter/material.dart';

class TeamFeed extends StatelessWidget {
  const TeamFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Team feed\n(Phase 1 placeholder)\n\nNext: show team updates/events + unread indicators.',
        textAlign: TextAlign.center,
      ),
    );
  }
}
