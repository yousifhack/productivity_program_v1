import 'package:flutter/material.dart';

class MyGuyFeed extends StatelessWidget {
  const MyGuyFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'My Guy feed\n(Phase 1 placeholder)\n\nNext: show my task updates/comments.',
        textAlign: TextAlign.center,
      ),
    );
  }
}
